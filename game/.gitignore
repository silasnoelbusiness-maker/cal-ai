# Godot 4 editor and import cache. Regenerated on first open / first import.
.godot/

# Exported builds
build/
export_presets.cfg

# Editor scratch
*.translation

# Dev screenshots: rendered on demand by tests/screenshot.gd and handed to
# whoever asked for them. Not build inputs, and not small.
/screenshots/
